<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ApplyMate.App.Views.Pages.ApplicationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid
        Padding="24"
        RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                FontSize="30"
                FontWeight="SemiBold"
                Text="Applications" />

            <Button
                Grid.Column="1"
                Command="{Binding AddApplicationCommand}"
                Content="Add Application" />
        </Grid>

        <Grid
            Grid.Row="1"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                Grid.Column="0"
                PlaceholderText="Search by company or job title"
                Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox
                Grid.Column="1"
                DisplayMemberPath="Label"
                ItemsSource="{Binding StatusOptions}"
                SelectedItem="{Binding SelectedStatus, Mode=TwoWay}" />

            <Button
                Grid.Column="2"
                Command="{Binding RefreshCommand}"
                Content="Refresh" />
        </Grid>

        <TextBlock
            Grid.Row="2"
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            Text="{Binding ErrorMessage}"
            TextWrapping="WrapWholeWords" />

        <ListView
            Grid.Row="3"
            IsItemClickEnabled="True"
            ItemClick="OnApplicationClick"
            ItemsSource="{Binding Applications}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock FontSize="16" FontWeight="SemiBold" Text="{Binding CompanyName}" />
                            <TextBlock Opacity="0.75" Text="{Binding JobName}" />
                        </StackPanel>

                        <Border
                            Grid.Column="1"
                            Padding="10,3"
                            Background="{Binding StatusBadgeBrush}"
                            CornerRadius="999">
                            <TextBlock FontWeight="SemiBold" Text="{Binding StatusText}" />
                        </Border>

                        <TextBlock
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Text="{Binding AppliedOnText}" />

                        <TextBlock
                            Grid.Column="3"
                            VerticalAlignment="Center"
                            Text="{Binding InterviewAtText}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
